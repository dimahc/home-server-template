all:
  children:
    homelab:
      hosts:
        homelab-server:
          # CUSTOMIZE: Set your server's IP address
          ansible_host: 192.168.0.10
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_ansible
          ansible_python_interpreter: /usr/bin/python3
    docker_hosts:
      hosts:
        homelab-server:
